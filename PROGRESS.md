# Progress Tracker

Target season: **2026** (training data 2015-2025).

## Master Plan Phase 1: Tier 1 Critical Fixes
- [x] Task 1.1: Centralized BLEND_RATIO_BY_POSITION into new src/constants.py, imported by team_aggregation.py/sos_adjustment.py/phase3_diagnostic.py (previously 3 separate copies, sos_adjustment.py's was missing "LB" - confirmed fixed, all three now reference the literal same dict object).
- [x] Task 1.2: Wired weekly updates -> game predictions. Generalized build_game_prediction_engine() to accept season/team_strength/schedule/weeks_after params (was hardcoded to 2026 only), added run_weekly_game_prediction_update() + validate_weekly_game_predictions() to weekly_predictions.py. Found and fixed a real bug while validating: the column-rename logic silently used the STALE pre-update offensive_strength column instead of the new _updated one (weekly_update_team_strength keeps both columns, so the original "not in columns" check never fired) - preseason and "updated" predictions were byte-identical until fixed. RESULT (real 2025, 4 checkpoints): spread std correctly WIDENS as expected (0.65->1.87 by week 1, up to 2.23 by week 12) confirming the compression-correction hypothesis - but game-level spread MAE is slightly WORSE for the updated version at every single checkpoint (e.g. 11.40 vs 11.30 after week 1, 11.99 vs 11.58 after week 12). Honest read: this doesn't contradict Subtask 2's original validation (which measured TEAM-level EPA accuracy and found real, monotonic improvement) - it shows that improvement doesn't cleanly translate to GAME-level point-spread MAE, and the MAE differences here (0.1-0.4 pts) are small relative to real single-game variance (~14 pts residual std from the game-level fit), so may not be a robust effect. Not spun as a win; flagged as a real, mixed result.

## Master Plan Phase 2: Compression Investigation
- [x] Task 2.1: Diagnosed. Decomposed the known ~3.3x compression ratio into stages (phase3_diagnostic.py: diagnose_player_level_compression, diagnose_team_level_compression, test_rescale_win_projections). FINDINGS: (1) model-level compression is real and position-dependent - QB 2.74x, RB 4.47x, WR 4.81x, TE 2.76x (real prior-season std vs. predicted std) - an expected regression-to-the-mean property of any R2<1 model, not obviously fixable without accepting worse per-player accuracy. (2) Team-level AGGREGATION adds further compression on top of the model's own: QB player-level predicted std=0.0657 roughly HALVES to 0.0328 after the starter-pick + play-mix blend - so it's not just the model, aggregation compounds it. (3) MOST IMPORTANT, genuinely counterintuitive finding: the rescale test (multiply net_strength around its mean by factors up to the full measured 3.34x "correction", refeed through the ALREADY-FITTED win model) shows MAE getting MONOTONICALLY WORSE as the rescale factor increases (2.882 wins at 1.0x -> 3.481 wins at 3.34x) - NOT better. Reason: correlation is only +0.216-0.33 depending on stage, so projected values are dominated by idiosyncratic noise, not true signal; rescaling amplifies the noise right along with the signal, and since noise dominates, MAE gets worse, not better. CONCLUSION: compression and weak correlation are different problems - you cannot fix accuracy by simply widening the bands, because the underlying signal is too weak for that to pay off. Any real fix has to target the correlation itself, not just the spread. This directly rules out "rescale/widen bands" as a viable Task 2.2 candidate fix.

## Master Plan Phase 2 Task 2.2: Test 5 Compression/Correlation Fixes
- [x] All 5 candidates tested (2 reconfirmed from already-completed work per user direction, 3 genuinely new). NONE beat the current best-known result (Task 3/Parallel B's availability-blended play-mix formula, +0.454). Summary: (1) bottom-up defense -0.064/-0.015 vs top-down +0.331, HURTS badly, reconfirmed; (2) R2-weighted aggregation +0.381, beats production's plain formula (+0.327) but below Task 3's best, not adopted; (3) recalibrating EPA->points on 2016-2024 + validating vs REAL 2025 (corrected from the spec's leaky 2016-2025-train/2024-holdout design) - slope barely moved (15.98->17.13, confirms no real drift) but real-2025 correlation collapsed to +0.078 (from 0.255 on 2024) - reflects 2025's inherent unpredictability (the same NE/JAX-type surprises that broke the season-level win projections), not a bad recalibration; (4a) median-of-top3-RB +0.442, within noise of current best, not better; (4b) QB+RB+WR blend still hurts (+0.233 vs QB-alone's +0.347), reconfirmed; (5) momentum/late-season-2024 form is WORSE at predicting next season (+0.311) than full-season average (+0.411) - 3 weeks is a noisier, smaller sample than 17; adding it as an extra feature improves R2 by a negligible +0.004. CONCLUSION per the plan's own stopping rule: no candidate improved correlation by the required +0.03 threshold - documenting that the underlying signal is genuinely weak rather than forcing an adoption, no team_strength files regenerated, proceeding to Phase 3 as specified.

## Master Plan Phase 3 Task 3.1: Season Win Inference from Game Predictions (Subtask 4)
- [x] Built infer_season_wins_from_game_predictions()/run_season_win_inference()/validate_season_win_inference() in win_projection.py. Generic over any game_predictions df, so it works for the 2026 deliverable (season_wins_from_games_2026.csv) and for a real-outcome-validatable 2025 equivalent (built by reusing build_game_prediction_engine(season=2025) unchanged, same generalization from Master Plan Phase 1 Task 1.2).
- 2026 RESULT: expected wins range only 7.45-9.29 (std=0.40 across all 32 teams) - EVEN MORE compressed than the season-level pipeline's own known compression (~1.0 std). Mechanically expected given Phase 2's findings: game-level win probabilities rarely stray far from 50% (max confidence 0.11, from Subtask 3), so summing 17 near-toss-up games barely differentiates teams.
- VALIDATION (real 2025, game-level aggregation vs. the old direct season-level EPA->win_pct pipeline): game-level-aggregated season wins scored corr=+0.003 (statistically no better than random) MAE=2.91, vs. the old season-level pipeline's corr=+0.216 MAE=2.88 - **the old season-level pipeline clearly wins**.
- CONCLUSION, resolving Phase 3 Redesign's open "two parallel systems" question from the original audit: do NOT use the game-level engine for season win totals - it's decisively worse for that purpose. Recommend keeping both systems for their respective strengths: game-level engine for individual-game spreads/Vegas comparison (what it was built and validated for), season-level EPA->win_pct pipeline for season win totals (what it's always been used for and remains best at). Phase 3 Redesign Subtask 4 is complete as a real, tested deliverable, but its own honest conclusion is "don't switch to this for win totals."

## Master Plan Phase 4: Tier 2 Cleanups
- [x] Task 4.1: Fixed phase3_diagnostic.py's stale hardcoded TE/SOS text. Deeper than prose - OFFENSE_PRED_COLS and load_real_2025_offense_epa() literally never included "TE" (hardcoded position lists), so Diagnostic 1 always printed "NO MODEL BUILT" regardless of reality. Added TE to both, removed the hardcoded stub block, made identify_unused_assets()'s TE line dynamic (checks accuracy_df instead of asserting), and corrected the SOS item to reflect the actual current finding (QB SOS hurts, not helps). Verified end-to-end: TE now shows real numbers (best column predicted_epa_per_play_synergy_adjusted, corr=+0.351).
- [x] Task 4.2: Split win_projection.py (5 unrelated jobs in ~700 lines) into epa_to_wins.py, backtest.py, game_predictions.py, and vegas_comparison.py (repurposed the empty placeholder stub into its real, correctly-named home rather than deleting it). win_projection.py kept as a thin re-export shim for backward compatibility. Only one real external dependency existed (weekly_predictions.py importing build_game_prediction_engine) - verified working through both the direct new-file import and the shim. Confirmed bit-for-bit identical output on 4 representative functions (game engine, Vegas comparison, edge detection, backtest) before/after the split - genuinely no behavior change.
- [x] Task 4.3: Removed dead code, confirmed via grep before deleting (not assumed): 17 legacy pre-EPA output files (qb/wr/rb_predictions_*.csv, cb/s/lb/edge_predictions_2026.csv, team_defense_epa_predictions_2026.csv, and 9 *_yards.pkl/*_tds.pkl/*_tackles.pkl/edge_sacks.pkl model files - all confirmed write-only, never read by any current code, though player_models.py still defines the legacy classes that produced them, left untouched); 13 stale non-"_corrected" team_strength/depth_scenario variants (pre-SOS-bug-fix numbers, superseded); src/win_prediction.py (confirmed zero references anywhere). vegas_comparison.py was NOT deleted per the audit's original suggestion - repurposed in Task 4.2 instead, resolving the "misleadingly named empty file" finding by making it non-empty and correct.

## Ensemble Approach: Part 1 - Season-Level Wins
- [x] Built src/ensemble.py. Fixed 3 spec issues before building: (1) Task 3/Candidate 2's offense-only correlations were never win predictions - paired each with the existing defensive Ridge model + fitted epa_to_wins model to get genuine wins; found and fixed a real bug this created (Candidate 2's different offensive_strength scale shifted the whole output to 12.2 avg wins through the fixed-intercept win model - fixed via scale-matching rescale, preserving the alternative formula's relative pattern); (2) Vegas's real corr vs actual 2024 games is +0.501 (not "+1.0 by definition"); (3) filled a real NaN (DEN missing from Candidate 2, its TE never cleared min_opportunities - known gap from Task 4) via the project's established fillna-with-mean convention.
- RESULTS (real 2025, 6 candidates: epa_wins_model, vegas_implied_wins, task3_formula_wins, candidate2_r2_weighted_wins, game_level_wins, naive_baseline): individual accuracy - Vegas clearly best (MAE=1.781, corr=+0.798), everything we've built clusters at MAE 2.7-2.9/corr 0.0-0.27. Combining rules: simple_average/median/trimmed_mean all WORSE than Vegas alone (dilutes its strength with our weaker models); weighted_average MAE=2.551 (still worse); vegas_anchored (50%) MAE=2.284 (still worse than Vegas's own 1.781!); stacking_loocv MAE=1.806 (closest, but still marginally worse than pure Vegas, not a real win - LOOCV likely just learned to put nearly all weight on Vegas).
- **HEADLINE FINDING: no combining rule beats using Vegas alone.** Matches the Phase 2 compression finding's spirit - averaging together several models whose errors are correlated (they share the same underlying EPA data) doesn't cancel error the way ensembling assumes.
- **SEPARATE, IMPORTANT FINDING: the chosen CI method (5th/95th percentile across the 6 raw candidate point estimates) is badly miscalibrated - 12.5% real coverage against a 90% target**, with intervals averaging only 1.94 wins wide. Root cause: percentile-across-ensemble-members only produces a valid interval if members are independent, unbiased draws from the outcome distribution - these are instead a handful of correlated, individually-compressed point estimates (the same compression problem from Phase 2), so their spread understates true uncertainty by roughly the same ~3x factor already documented. Flagged before building Part 2 (game-level), since the same CI method was specified there too - needs a different approach (e.g. the already-precedented residual-based band from epa_to_wins.py, not spread-across-correlated-estimates) before it can be trusted.

## Phase A Component 1: Elo Model
- [x] Built src/elo_model.py. Corrected 4 spec issues before building: (1) spec's "EPA baseline corr=+0.306" traced to a stale, superseded, DIFFERENT metric (offensive_strength-vs-real-offensive-EPA from the SOS-bug-fix entry, itself later superseded to +0.327) - not a win-projection accuracy figure at all. Real current baseline (ensemble.py): corr=+0.216, MAE=2.88 (MAE was already right in the spec, only corr got swapped). (2) schedules_2025.csv doesn't exist standalone - used game_predictions._load_schedule_for_season() instead. (3) No preseason Vegas win-total market exists in this data (already established) - used compute_vegas_implied_wins() (devigged per-game moneyline sum) as the Vegas signal instead, translated to Elo via a points-per-win scale GRID-SEARCHED on real 2015-2024 games (not asserted, unlike nfelo's fixed constant). (4) Cross-season carryover wasn't addressed in the spec - resolved by resetting the Vegas-informed variant fresh each season (its own Vegas line already encodes personnel changes) while building a SEPARATE continuous-Elo path with 1/3 regression-to-mean between seasons for the non-circular test (see below). Also added real home-field advantage (empirically fit, +32.4 Elo points from the real 54.7% home win rate, 2015-2024) since the spec's formula had none.
- **Caught and fixed a flaw in my own first validation pass, not the spec's**: the initial "neutral, non-circular" test reset every team to a flat 1500 for the whole 2025 season with zero in-season updates - zero information in, so it trivially produced ~0 correlation. That's a broken test, not a finding about Elo. Fixed by building `run_multi_season_elo()` - a continuous Elo run across 2015-2025 with NO Vegas signal anywhere, regressed 1/3 toward the mean at each season boundary (disclosed as an assumed standard constant, not fit - no pre-2015 season exists to validate the very first reset against) - giving a real carryover rating built purely from win/loss history.
- RESULTS (real 2025; learned K=10, points_per_win=45, home_field_elo=+32.4, all via grid search/regression on real 2015-2024 games):
  - (a) Vegas-informed preseason Elo: corr=+0.828, MAE=1.71 wins - technically beats even the raw Vegas benchmark (+0.798/1.78), but this is EXPECTED/CIRCULAR (it imports Vegas's own number as the starting rating) - re-confirms Vegas beats EPA, not that Elo-the-method is good.
  - (b) **Carryover Elo (real 2015-2024 win/loss history only, no Vegas anywhere) - the real, non-circular test: corr=+0.316, MAE=2.74 wins. This genuinely beats the real EPA baseline (+0.216 corr, 2.88 MAE) on both metrics** - a simple win/loss-history rating beats the much more complex position-by-position EPA pipeline.
  - (c) In-season game-by-game accuracy: Brier=0.2011 (holdout, vs. 0.2036 train - holds up out-of-sample), corr(predicted_prob, actual_result)=+0.439 - notably better than the existing game-level EPA->points regression's own 2024 holdout corr of +0.255 (game_predictions.py).
- **IMPORTANT, SURPRISING FINDING - the spec's core premise is wrong.** Checked compression (the spec's stated rationale for building Elo at all: "naturally avoids 3.34x compression"): carryover Elo's projected-wins std is 0.71 vs. real 2025 std of 3.46 (~4.9x too compressed) - actually WORSE than the EPA model's own 3.36x compression (std 1.03). Elo is not "naturally" less compressed; it still beats EPA on ranking accuracy (MAE/corr) despite being MORE compressed - an interesting, non-obvious result, but the compression problem (Phase 2's finding) is not solved by switching to Elo.
- Known limitations: grid resolution is coarse (K in 10/20/30/40/50, points_per_win in 15/25/35/45 - true optimum may be off-grid, note K=10 won the search at the low end of the tested range); the 1/3 season-to-season regression constant is an assumed convention, not fit; elo_uncertainty is currently a fixed placeholder, not a calibrated quantity.

## Phase A Component 2: EPA-Elo Ensemble
- [x] Built src/ensemble_model.py. Corrected 2 issues before building: (1) Component 1 produced two Elo variants (Vegas-informed, circular; carryover, the genuine independent signal) and the spec didn't say which to use for "EPA-Elo" - used carryover, since using Vegas-informed here would make this secretly an EPA-Vegas ensemble and double-count Vegas ahead of Component 3, which blends with Vegas explicitly. (2) learn_optimal_weights() as specified would fit the blend weight on the same 2025 outcomes validate_ensemble_backtest() then scores against - the identical leakage class already caught once in ensemble.py's combine_stacking_loocv. Fixed with leave-one-team-out CV: each held-out team's weight is chosen from the other 31 teams only.
- RESULTS (real 2025): EPA alone corr=+0.216 MAE=2.88 | Carryover Elo alone corr=+0.316 MAE=2.74 | LOOCV-learned ensemble weight: **w_epa=0.00, w_elo=1.00 in all 32 of 32 leave-one-out folds** - a completely clean, unanimous result, not a close call.
- **HEADLINE FINDING: the ensemble collapses to pure Elo - EPA contributes nothing when blended with carryover Elo.** LOOCV MAE = 2.74 wins, identical to Elo alone (not an improvement, a tie by collapse). This mirrors ensemble.py Part 1's finding (no combining rule beat the single strongest component alone) with Elo now playing the role Vegas played there. Interpretation: EPA and Elo aren't independent enough / EPA isn't accurate enough to add anything once Elo's win/loss-history signal is already included - simpler beats combined again, same theme as the original Parallel Exploration's conclusion.
- 2026 ensemble predictions generated (effectively = 2026 carryover Elo, since w_epa collapsed to 0) - saved but NOT validated (2026 unplayed).
- Known limitation: weight grid is 0.05-resolution; a completely unanimous 32/32 collapse to the boundary makes finer resolution unlikely to change the conclusion, but wasn't separately verified.

## Fantasy Direction Validation
- [x] Built src/fantasy_validation.py. Corrected 4 spec issues before building: (1) Steps 1-2 ("parse PBP into fantasy points from scratch", "estimate usage, EPA x usage x asserted conversion factor") both reinvent things that already exist - player_weekly_stats.csv already has real per-player-per-week fantasy_points_ppr (2015-2025), and {qb,rb,wr,te}_epa_projections_2025.csv already ARE this project's real, leak-free, prior-season-based preseason player projections. Reused directly. (2) Spot-checked the existing fantasy_points_ppr column against the spec's formula on 3 real week-1 QBs before trusting it - every term matched except passing TDs (file scores 4pts, spec said 6pts, a real discrepancy, not a bug - 4pt is the far more common ESPN/Yahoo/Sleeper default). **Asked the user; chose to use the existing 4pt-passing-TD column as-is.** (3) QB uses the OL-adjusted column, not SOS-adjusted, per this project's own established finding that QB SOS-adjustment hurts (Production Update task, this session). (4) projected_volume = opportunities_prior_season * (expected_games_2025/17) - disclosed as using a RAW, not per-game-normalized, 2024 total.
- RESULTS (real 2025, PPR): QB n=42 corr=+0.435 MAE=87.2pts top10=30% | RB n=83 **corr=-0.504** MAE=67.2pts top10=0% | WR n=107 corr=+0.591 MAE=51.3pts top10=40% | TE n=44 corr=+0.436 MAE=46.4pts top10=50%.
- **Investigated the RB result before reporting it, since a strongly NEGATIVE correlation (not just weak/noisy) was surprising enough to warrant checking for a bug rather than a real finding.** Confirmed real, not a bug (Christian McCaffrey case-checked directly): his opportunities_prior_season=69 and expected_games_2025=8.7 are both crushed by his real, injury-shortened 2024 season, projecting him as a non-factor - he then had a full healthy bounce-back 2025. This is a real, well-known fantasy-football phenomenon (RB is the most year-over-year-volatile skill position, especially for injury-recovery/bounce-back candidates), not a code defect.
- **DEEPER, GENERALIZABLE FINDING (decomposed each position's formula into its EPA-alone vs. opportunities-alone components before finalizing the report):** across ALL FOUR positions, raw prior-season opportunity volume ALONE correlates with real 2025 fantasy output about as well as or better than the full EPA x volume formula (QB: epa-alone +0.417 vs. opportunities-alone +0.506 vs. combined +0.435; WR: +0.349 vs +0.554 vs +0.591; TE: **-0.109** vs +0.644 vs +0.436; RB: +0.090 vs **+0.667** vs **-0.504**). For RB and TE specifically, epa_per_play_sos_adjusted ALONE is weak-to-negative, and multiplying it into the volume term makes things WORSE than volume alone would have been (dramatically so for RB). This is the same "simpler beats combined" pattern found repeatedly this session (Ensemble Part 1, Component 2's Elo collapse) showing up again at the player level - the EPA "quality" signal isn't reliably adding value once real opportunity volume is already known, and for RB it actively subtracts value.
- RECOMMENDATION: **MIXED, with real position-specific nuance** (not a clean pursue/shelve per the spec's own thresholds: 3/4 positions cross the +0.40 "green" bar, but 0/4 clear the 50% top-10 bar, and RB is actively broken). WR is the strongest, genuinely viable candidate as specified. QB and TE are borderline. RB needs a different formula (raw opportunities_prior_season alone outperforms the specified EPA x volume approach by a wide margin) before it could be trusted - not evidence EPA-based fantasy rankings are hopeless, but evidence the SPECIFIED formula is the wrong one for at least one position.
- Skipped Defense/IDP (spec marked it optional/"if time"; no existing infra to reuse, out of scope for a quick test).

## Game Prediction Engine Improvement, Test A: Elo for Spreads
- [x] Built src/elo_game_prediction.py. Corrected 3 spec issues before building: (1) the spec's own instructions contradicted each other - "use carryover Elo" but "load elo_ratings_2025.csv", and that file was actually built with Component 1's VEGAS-INFORMED Elo (validate_elo_backtest's run_elo_season call defaults to use_vegas_signals=True) - exactly what the spec said not to use. Regenerated genuine carryover Elo fresh via elo_model.run_multi_season_elo() instead of loading that file. (2) The spec's win-probability formula claimed home-field advantage was "already baked into home_elo" from Component 1 - false, it's applied separately at prediction time (+32.4, empirically fit) - reused Component 1's real function instead of a formula that would've silently dropped home-field advantage. (3) data/processed/game_predictions_2024_epa.csv doesn't exist - reconstructed the real 2024 EPA holdout game predictions locally via the same merge fit_game_level_epa_to_points() already does internally, instead of skipping the game-by-game comparison.
- Caught and fixed a real bug in my own first draft: initially passed home_field_elo=0.0 into run_multi_season_elo() to "avoid double-counting" it against the separate prediction-time home_field_elo - wrong reasoning (Component 1 uses the same home_field_elo consistently in both the rating-update step and the prediction step; they're two uses of one formula, not additive). Fixed before running - would have produced Elo ratings inconsistent with Component 1's real, validated ratings.
- RESULTS (2024 holdout, form=linear won a near-exact 3-way tie with logit/normal): **Elo corr=+0.393 MAE=10.21 pts vs. EPA baseline corr=+0.255 MAE=10.82 pts. CI coverage=89.3% (target 90%, well-calibrated - unlike the percentile-based CIs found miscalibrated elsewhere this session, this one is a direct residual-std band from a real regression fit, and it works).** Replicated cleanly on real 2025 (context only, actually the cleaner double-holdout of the two - untouched by both the Elo-hyperparameter fit and this new conversion fit): corr=+0.385 MAE=10.36, CI coverage=89.0%.
- **HEADLINE FINDING: Elo decisively beats EPA for game spreads too, not just season wins.** Verdict: GREEN per the spec's own success criteria. Game-by-game: Elo won 150/272 (55.1%) vs. EPA 122/272 (44.9%).
- **INTERESTING PATTERN FOUND (not asked for by the spec's minimum bar, but directly informs the "hybrid?" question it raises): Elo's edge over EPA is NOT uniform.** By |Elo differential| quartile, Elo's win rate is 41.2% (closest games) / 54.4% / 63.2% / 61.8% (biggest mismatches) - EPA is actually slightly BETTER than Elo in the most competitive, closest-rated games, and Elo's advantage grows with the size of the talent gap. Worth considering for a future hybrid (Elo for confident/lopsided calls, EPA or an ensemble for genuine toss-ups) rather than a full Elo replacement - flagged as a real, evidence-based option, not built here (out of scope for this test).
- Known limitation (disclosed, not fixed): Elo's own hyperparameters (K=10, points_per_win=45, home_field_elo=+32.4) were fit on 2015-2024 in Component 1, so 2024 isn't a completely untouched holdout for the Elo MECHANISM itself - only for the new probability->spread conversion (genuinely fit fresh on 2015-2023 here). 2025's clean replication is the stronger evidence of the two.

## Game Prediction Engine Improvement, Component B: Weekly Recalibration Feedback Loop
- [x] Built src/weekly_recalibration.py. Corrected 3 spec issues before building: (1) Function 2 said to call weekly_update_pipeline() for "updated Elo ratings" - that's the EPA team_strength shrinkage-blend mechanism, unrelated to elo_model.py, and doesn't return anything Elo-shaped - a category error. Elo's K-factor update rule already IS a per-game update mechanism (unlike EPA/team_strength, which needed weekly_update_pipeline() specifically because it has none of its own) - "updated Elo through week N" is extracted directly from Component A's already-built, already-causal Elo chain instead. (2) elo_ratings_2025.csv (listed as "preseason Elo" input) is Vegas-informed and in-season chained, not preseason - not used, consistent with Component A. (3) elo_game_predictions_2026.csv (listed as "preseason spreads") is for the WRONG SEASON (2026, not 2025) - built a genuine static 2025 preseason baseline directly instead.
- RESULTS (real 2025, freeze-Elo-at-week-N-and-project-all-remaining-games methodology): corr +0.248 (wk1, 256 games) -> +0.275 (wk4) -> +0.297 (wk8) -> +0.369 (wk12) -> +0.412 (wk16, 32 games) - **correlation improves monotonically, confirming the core hypothesis**. MAE: 11.17 -> 11.24 -> 10.81 -> 11.42 -> 10.89 - NOT monotonic, bounces in a narrow band, most likely small-sample noise as the remaining-games count shrinks to 32 by week 16 (reported honestly rather than forcing a false "improving" narrative). Avg |spread change from preseason| grows monotonically: 0.52 -> 1.07 -> 1.70 -> 2.36 -> 2.97 pts (spreads revise progressively more as real data accumulates, as expected).
- **IMPORTANT METHODOLOGICAL FINDING, caught by comparing these numbers to Component A's own reported "2025 context" figure (corr=+0.385) before finalizing the report - the two are NOT the same thing despite both being "2025 Elo accuracy":** Component A's number uses each individual game's own fully look-ahead-chained pre-game rating (chained all the way up to that SPECIFIC game, using every real result before it, including results between the checkpoint and that game). This module's numbers use a SINGLE frozen snapshot at week N applied to ALL remaining games through week 18 - the real, live-deployment-realistic scenario (a bettor in week 1 does NOT get to use week-11-updated ratings to predict a week-12 game). This is why Week 1's corr here (+0.248) is meaningfully lower than Component A's aggregate (+0.385) - Component A's number has a look-ahead advantage this module correctly does not give itself. This module's methodology is the more honest one for "does recalibration help a live user," and the correction is disclosed rather than left as an unexplained discrepancy between two same-named numbers.
- CI width is intentionally flat across checkpoints (not a bug) - per-game spread uncertainty isn't a season-elapsed quantity the way season-TOTAL projection uncertainty is; a close game in week 1 and a close game in week 16 have comparable variance.
- Verdict: GREEN-leaning-MIXED per the spec's own criteria (correlation trajectory and spread-delta trajectory both cleanly green; MAE trajectory noisy rather than cleanly improving, most likely a small-sample artifact at later checkpoints, not a sign the mechanism is broken).
- Infrastructure confirmed ready for live 2026 (prepare_for_live_2026() documents the process); known edge case flagged but untested: a new/relocated franchise with no real prior-season history has no defined Elo seed (none exist in 2015-2026 data, so this has never been exercised).

## Game Prediction Engine Improvement, Component C: Vegas Integration (LOOCV-Learned Weights)
- [x] Built src/vegas_integration_optimized.py. Corrected 3 spec issues before building: (1) the grid search range [0.60, 0.95] contradicted the spec's own hypothesis (Vegas weight reaching ~12% by week 16, mathematically unreachable with a 0.60 floor) - widened to the full [0.0, 1.0] so the data could actually show a low late-season weight if real. (2) The LOOCV pseudocode never evaluated the held-out week - it picked weights from training weeks' own MAE and just labeled that "optimal for week W" without ever checking week W itself. Same missing-held-out-evaluation gap already caught twice this session (ensemble.py stacking, Component 2 weight learning) - fixed by scoring the chosen weights against the real held-out week too. (3) vegas_spread uses the real, direct spread_line column (what oddsmakers post), not a moneyline-derived proxy - more direct and consistent with vegas_comparison.py's verified sign convention.
- RESULTS (real 2025, corrected [0,1] grid): **every one of the 5 LOOCV checkpoints (weeks 1, 4, 8, 12, 16) independently chose w_vegas=1.00, w_elo=0.00 - Vegas alone, at every single checkpoint, including week 16.** Held-out week MAE: wk1=5.94, wk4=9.03, wk8=14.35, wk12=7.07, wk16=10.62 (noisy, small per-week samples of 13-16 games, expected). Confirmed why directly: real Vegas spread_line alone scores corr=+0.504/MAE=9.72 (2025) vs. carryover Elo alone corr=+0.385/MAE=10.36 (Component A) - Vegas is simply the stronger signal at every point in the season tested, so blending in weaker Elo never helps.
- **The spec's own hypothesis (Vegas weight decaying from ~87% to ~12% over the season) was tested directly and FALSIFIED by the real data - not a modeling failure, a genuine finding.** The learned schedule is flat: 100% Vegas, 0% Elo, all 18 weeks. This is the clearest version yet of a pattern that's now appeared four times this session (Ensemble Part 1 collapsed to Vegas; Component 2's EPA-Elo ensemble collapsed to Elo; here it collapses to Vegas again) - simple weighted blending of a strong signal with a weaker, related one doesn't help; it dilutes the strong signal.
- Per the spec's OWN stated success criteria (learned beats arbitrary linear by >2% corr or >0.2 MAE): **GREEN** - learned (=pure Vegas) scores corr=+0.504/MAE=9.72 vs. arbitrary linear (80/20->20/80) corr=+0.461/MAE=10.02 (delta +0.043 corr, -0.29 MAE, both clear the bar). The curve-fit step is moot here (all 5 learned points are identical at 1.00, so R2 is undefined/NaN - correctly reported, not a bug: a flat "curve" has zero variance to explain).
- 2026 predictions generated but real Vegas lines are very sparse this far out (53/272 games have a posted spread_line for the earlier "vegas_comparison_framework" check; **0/272 by the time this task's data pull ran** - books hadn't posted any 2026 lines yet at all) - all 272 games fall back to Elo-only, clearly flagged via has_vegas_line, not fabricated. Will need re-running closer to the season once real 2026 lines post; per the learned schedule, they'll get 100% weight once available.
- Recommendation: deploy Vegas-only (not a blend) wherever a real line exists; fall back to Elo-only where it doesn't. The elaborate learned-weight-curve machinery is real, correct infrastructure, but the real answer it found is simpler than either the spec's hypothesis or the original arbitrary schedule: don't blend, just prefer Vegas when available.

## Dynamic Season-Win Tracking
- [x] Built src/dynamic_tracking.py. Corrected 3 spec issues before building: (1) Step 1's "wiring" was mostly already done - run_weekly_game_prediction_update() (weekly_predictions.py, built in Phase 3 Redesign Subtask 2 / Master Plan Task 1.2) already chains weekly_update_pipeline() -> build_game_prediction_engine() restricted to the remaining schedule; only actual-wins-through-week-N and the actual+predicted combination were actually missing. (2) The spec's proposed CI (flat epa_to_wins.py resid_std_wins ~=2.0, a FULL 17-game-season residual, applied regardless of games remaining) would badly under/overstate uncertainty depending on the checkpoint - used the existing per-remaining-game Bernoulli variance from infer_season_wins_from_game_predictions() instead, which already scales correctly with games left. (3) "Compare to Vegas trajectory" isn't buildable - vegas_with_results_2015_2025.csv has closing lines only, no odds-movement history, so the spec's own per-week Vegas numbers were already labeled "(est)" (invented) - substituted a single fixed Vegas-implied-wins benchmark instead. Also fixed a real perf issue before running: naive per-checkpoint calls would each re-trigger estimate_prior_weeks()'s 8-candidate x 3-checkpoint search (each a chunked 1.3GB PBP read) - computed it once (prior_weeks=3, empirically chosen) and passed it through all 5 checkpoints.
- RESULTS (real 2025, checkpoints week 1/4/8/12/16, fixed Vegas benchmark corr=+0.798 MAE=1.78):

  | Week | MAE | corr | CI coverage (target 90%) | avg CI width | vs. fixed Vegas benchmark |
  |---|---|---|---|---|---|
  | 1 | 2.82 | +0.069 | 59.4% | 6.51 wins | below |
  | 4 | 2.41 | +0.550 | 62.5% | 5.87 wins | below |
  | 8 | 2.14 | +0.699 | 59.4% | 5.00 wins | below |
  | 12 | 1.43 | +0.867 | 75.0% | 3.94 wins | beats |
  | 16 | 0.64 | +0.974 | 90.6% | 1.98 wins | beats |

- **HEADLINE FINDING: MAE improves monotonically and correlation climbs from +0.07 to +0.97 as real results accumulate - the spec's core hypothesis is confirmed.** This validates the "Vegas is at +0.798 because it updates, not because of preseason magic" theory: our own preseason-only number was corr=+0.216 (Task 4.1), and this tracker matches/beats the fixed Vegas benchmark by week 12 - not because our remaining-game predictions got dramatically better, but because actual, error-free results increasingly dominate the total as the season progresses. This is expected and mechanical, not a claim that our remaining-game prediction quality now exceeds Vegas's.
- **CAVEAT on "beats Vegas" - not an apples-to-apples claim.** The Vegas benchmark used here is a single fixed closing-line number (see spec correction #3 above - no real weekly Vegas trajectory data exists to compare against). A real, continuously-updating Vegas line would also improve as the season progresses; this only shows our tracker beats a STATIC preseason-quality reference, not that it beats a live market.
- **SEPARATE FINDING: CI coverage is itself dynamic and starts badly miscalibrated.** 59-63% coverage (vs. 90% target) through week 8, improving to 90.6% by week 16. Root cause: the Bernoulli win-probability variance used for the CI only captures game-outcome randomness GIVEN a correct win probability - it does not capture the team-strength model's own point-estimate error (bias), which dominates early in the season when many games remain and shrinks to near-irrelevant by week 16 when only 1-2 games are left. This is a more benign version of Part 1's CI finding (12.5% coverage, never recovering) - here the CI self-corrects as the season progresses and actual results replace model-dependent predictions, but it should not be trusted as calibrated in the first half of a season.

## Phase 3 Redesign: Weekly Update Pipeline
- [x] Subtask 2: Built as `src/weekly_predictions.py` (was an empty placeholder file from early scaffolding - reused rather than creating a new module). Confirmed the real blocker before building: nflreadpy's load_pbp() raises ValueError for season=2026 (season hasn't started), so there's no real week to process yet - built the real, season/week-parameterized mechanism and validated it against the fully-completed real 2025 season instead, per direction. Team-level shrinkage update (empirical-Bayes blend of preseason team_strength with real partial-season EPA, weighted by weeks played), not full player-level cascade - the original spec's own player-level functions were unfinished stubs, so this is real working functionality where the spec had none. prior_weeks=3 empirically estimated (tested 1-14 against real 2025 final outcomes at weeks 4/8/12, not asserted). VALIDATION (real 2025, vs. static preseason MAE=0.071/corr=+0.327): after week 1 the update is slightly WORSE than preseason (0.073 vs 0.071, expected - 1 week is noisy, shrinkage correctly limits the damage but can't fully avoid it); from week 2 onward it consistently BEATS the static preseason projection and improves monotonically (MAE 0.053->0.018, corr +0.68->+0.98 by week 16) as real season data increasingly dominates the blend. A naive no-shrinkage "partial season alone" baseline is dramatically worse at week 1 (MAE=0.171 vs the shrunk 0.073) but converges to nearly identical by week 4+ - confirms the shrinkage mechanism's value is concentrated exactly where theory predicts, in the earliest, smallest-sample weeks.

## Phase 3 Redesign: Vegas Comparison & Edge Detection
- [x] Subtask 3: `vegas_comparison_framework()`/`identify_edges()` built in win_projection.py. `load_vegas_lines_all_2026()` doesn't exist - real lines just live on schedules_2026.csv directly. Checked before building: books have only posted real 2026 lines through weeks 1-4 so far (53/272 games) - genuinely how far out sportsbooks publish lines this far before the season, not a bug, so this compares to REAL currently-live market data rather than needing a historical simulation (unlike Subtask 2). Reused moneyline_to_implied_prob() (Task 5) instead of repeating that task's already-fixed over/under-as-win-total bug. Verified spread_line's sign convention against real historical blowout games before use (positive = home favored, matches our own expected_spread convention - no flip needed). RESULTS: avg spread disagreement -0.61 pts, corr(our spread, Vegas spread)=+0.257, corr(win prob)=+0.247 - modest agreement, expected for a first-pass game model. Biggest disagreements: ARI/SEA week 2 (we say +2.4, Vegas -10.0), LAC/ARI week 1 (we say +1.0, Vegas +11.5). EDGE DETECTION: 0 edges found at confidence>0.15 threshold - not a bug, our model's win probabilities never exceed 0.11 confidence (max, across all 53 games) because of the compression issue already flagged repeatedly in this project (Task 4.1/Task 5's Vegas validation) - the game engine's spread std (1.10 pts, from Subtask 1) is tiny next to real game-level variance (~14 pts), so it can never express high single-game confidence as currently calibrated.

## Phase 3 Redesign: Game Prediction Engine
- [x] Subtask 1: Game prediction engine built (`win_projection.py::build_game_prediction_engine`). Pre-req fix: rebuilt team_strength_2026.csv with baseline QB (was still using SOS-adjusted from before today's Production Update task existed). Corrected the spec's 4 asserted constants (EPA_TO_POINTS=3.5, HOME_FIELD_ADVANTAGE=2.5, league_avg_points=21.0, win-prob std_dev=5.0) into ONE real regression on 2,017 real historical REG games (point_diff ~ epa_diff, 2016-2023): slope=15.98 pts/EPA-unit, intercept=+1.64 pts (real home-field advantage), residual std=14.04 pts, real league-avg total=45.42 pts/game - all empirically derived, none asserted. 2024 holdout: MAE=10.82 pts, corr=+0.255 (modest, as expected - single-game outcomes are inherently much noisier than season win totals). Generated predictions for all 272 real 2026 REG games. Known simplification, disclosed not hidden: expected_total is currently constant across all games (league average) since only the point DIFFERENTIAL was modeled, not each team's individual scoring level - a real matchup-specific total (e.g. two elite defenses = lower total) isn't captured; same structural limitation the original spec's design had.

## Prep Task: Refresh 2025 Data & Rebuild 2026 Offense Projections
- [x] Environment migrated to D: (C: drive was ~3MB free, blocked all installs) - new venv at D:/venvs/nfl-model, old C: conda env untouched. See memory: env-python-interpreter.
- [x] nflreadpy confirmed to solve nfl_data_py's 2025 season/weekly-stats 404 (defense/PFR/PBP sources already had 2025 - only offense weekly/seasonal was blocked). Pulled real 2025 offense data, mapped nflreadpy's schema onto the existing raw-cache column set, appended to player_weekly_raw/player_seasonal_raw, reran run_cleaning_pipeline() unchanged (same code path as every other season).
- [x] Rebuilt player_features_with_history.csv + age curves (run_feature_engineering()) with 2025 included.
- [x] Regenerated QB/RB/WR/TE 2026 baseline projections using the already-trained pretrained models with ref_season=2025 (no retraining - same reuse-across-ref-seasons convention as Task 4's historical backtest).
- [x] Applied OL/SOS/availability adjustments to all four positions for 2026.
- [x] Built team_strength_2026.csv (offense: Task 3's winning availability-blended play-mix formula, ref_season=2025; defense: top-down team_defense_epa model, ref_season=2025 - genuinely 2026-targeting, unlike the existing 2025-target leak-free defense rebuild in team_aggregation.py). No real-2026 validation possible yet (season unplayed).
- **BUG FOUND (pre-existing, affects the whole project's SOS history, not just this task)**: `sos_adjustment.build_component_models()` hardcodes `ref_season=HOLDOUT_SEASON-1` (2023), contradicting its own docstring ("projects... to 2025 from 2024 data") - an off-by-one that means every SOS-adjusted projection ever produced by `run_sos_adjustment()` (Task 5.1 onward, including this session's TE/Phase-3-Rebuild work) used opponent-quality data projected for 2024 (from 2023), not 2025 (from 2024) as intended. Worked around for this task's 2026 pipeline by calling `train_defense_component_model`/`predict_next_season` directly with the correct `ref_season`; NOT fixed in `build_component_models()` itself or rerun retroactively for the 2025 pipeline, since that would silently change already-reported 2025 results without asking first.
- **BUG FOUND AND FIXED (this task)**: `build_team_opponents(schedules_2015_2025.csv, 2026)` silently returned 0 rows (that file has no 2026 games), which would have corrupted every 2026 SOS-adjusted column to NaN. Fixed by pulling the real 2026 schedule directly (272 REG games, confirmed already released) and saving it as `data/raw/schedules_2026.csv`.

## SOS ref_season Bug Fix - Rerun 2025 Pipeline
- [x] Fixed `sos_adjustment.build_component_models()`'s off-by-one (`ref_season=HOLDOUT_SEASON-1` -> `ref_season=HOLDOUT_SEASON`, i.e. 2023->2024) and reran everything downstream: SOS pipeline, team_strength_2025.csv, win_projection.py (both run_win_projection and run_backtest_comparison), Phase 3 Diagnostics 1+2.
- **HEADLINE RESULT - QB SOS-adjusted verdict FLIPS**: with the corrected (fresh) 2024 opponent data, QB SOS no longer helps real 2025 accuracy (MAE 0.0828->0.0844, worse; individual-player corr 0.386 vs baseline's 0.403) - the opposite of the original (buggy, stale-2023-data) "HELPS" verdict that Task 5.1, production's `build_offensive_team_strength()`, and every downstream Phase 3 Rebuild artifact (Task 3's depth scenarios, Parallel Task B's simple formula, Improvement Task 1's combined result, Parallel Task A's full-roster) all built on top of. TE SOS still helps (slightly better than before: 0.1959->0.1925). WR/RB SOS still don't help (unchanged verdict).
- Also fixed a second, unrelated pre-existing bug found while regenerating diagnostics: `phase3_diagnostic.measure_component_contributions()` was called with a `real=` kwarg its signature didn't declare (an incomplete refactor from earlier this session) - added the missing parameter.
- Net effect on already-reported numbers: production offensive_strength corr +0.341->+0.306, net_strength +0.303->+0.283; Phase 4 backtest win corr ~unchanged (0.224->0.221); Diagnostic 2's "QB-alone beats QB+RB blend" conclusion still holds directionally (+0.361->+0.335, still beats current formula's now-+0.306).
- **NOT done, flagged for a follow-up decision**: Task 3/Parallel Task A/Parallel Task B/Improvement Task 1's saved CSVs (`team_strength_2025_availability_adjusted.csv`, `_simple.csv`, `_combined.csv`, `_full_roster.csv`, `depth_scenario_qb.csv`) all still embed the OLD (buggy) SOS-adjusted QB values and were not regenerated - out of this task's explicit scope, but now stale. Also not addressed: production's `build_offensive_team_strength()` still hardcodes QB=SOS-adjusted even though that adjustment no longer validates - worth reconsidering (e.g. switch to baseline or OL-adjusted) as a separate decision.

## Phase 3 Rebuild Regeneration (post SOS bug fix)
- [x] Subtask 1: Task 3 depth scenarios rebuilt. `project_team_strength_scenarios()` generalized with a `qb_canonical_col` param (default changed from SOS-adjusted to baseline `predicted_epa_per_play`, since SOS no longer validates for QB - see SOS bug fix task). Isolated both effects separately: fixing just the stale-data bug (QB still SOS-adjusted) gives full_season +0.306/availability_adjusted +0.439/backup_only +0.374 (all below the original buggy numbers - confirms the bug was inflating them); fixing the bug AND switching QB to baseline gives +0.327/+0.454/+0.376 - better than data-fix-alone across the board, confirming baseline is the right column now. availability_adjusted remains the best scenario within Task 3 (+0.454, essentially matching the old +0.464 despite the underlying bug fix - backup_only is even slightly better than before, +0.376 vs +0.374).

- [x] Subtask 2: Task 4 weight optimization rechecked - UNCHANGED. Confirmed (not assumed) by rerunning: `optimize_offensive_weights_via_regression()` never used QB's SOS-adjusted column (always baseline `predicted_epa_per_play`, both for the historical 278-team-season training pool and the real-2025 holdout), so it was never exposed to the SOS ref_season bug. All four stepwise model results and the winning weights (qb=0.327/rb=0.169/wr=0.290/te=0.273, corr=+0.368) are bit-for-bit identical to the original run.

- [x] Subtask 3: Parallel Task A (full-roster depth) rebuilt. `build_full_roster_aggregation()` generalized with the same `qb_canonical_col` param/default-to-baseline fix as Subtask 1 (WR's SOS-adjusted column left as-is - separate, pre-existing "NO HELP" finding unchanged by today's fix, out of scope here). Isolated both effects: fix-only (QB still SOS-adjusted) = +0.363; fix + QB baseline = +0.366. Both below the original buggy +0.376 - unlike Task 3, switching to baseline doesn't fully recover the gap here, since QB is a smaller fraction of this blend (weighted alongside RB/WR/TE via Task 4's optimized weights, not 100% QB+RB like Task 3's play-mix formula) so its column choice matters less. Compression ratio unchanged (2.41x vs. previous 2.39x).

- [x] Subtask 4: Parallel Task B (simple formula) rebuilt. Same qb_canonical_col fix as Subtask 1/3. Results: fix-only +0.439, fix+baseline +0.454 - exact match to Subtask 1's numbers, confirming (again) this formula is mathematically identical to Task 3's availability_adjusted scenario. availability_adjusted/simple-formula remains the best-performing single approach across the whole rebuild.

- [x] Subtask 5: Improvement Task 1 (combined) rebuilt. Same fix pattern. Results: fix-only +0.440, fix+baseline +0.442 - barely moved from the original +0.444 (QB diluted by the multi-position blend here too, same pattern as Parallel A).

### FINAL SUMMARY - Phase 3 Rebuild Regeneration (all 5 subtasks)
| Approach | Old (buggy) | New (fix only, QB still SOS-adj) | New (fix + QB baseline) |
|---|---|---|---|
| Task 3: availability_adjusted | +0.464 | +0.439 | **+0.454** |
| Task 4: optimized weights (QB+RB+WR+TE) | +0.368 | +0.368 (unaffected - never used SOS-adjusted QB) | +0.368 |
| Parallel A: full-roster depth | +0.376 | +0.363 | +0.366 |
| Parallel B: simple formula | +0.464 | +0.439 | **+0.454** (identical formula to Task 3, confirmed exact match both times) |
| Improvement 1: combined | +0.444 | +0.440 | +0.442 |
| Production (build_offensive_team_strength - NOT updated to baseline QB) | +0.341 | +0.306 | not rebuilt with baseline - still uses SOS-adjusted QB |

**Winner unchanged**: Task 3 / Parallel B (identical formula) remain the best approach at +0.454, just barely below their pre-fix (inflated) +0.464. The original Parallel Exploration's headline conclusion - "simplicity beats complexity," availability-blended QB+RB with a plain play-mix formula beats every fancier multi-position/full-roster approach tried - **survives the bug fix intact**, just with slightly more honest numbers.

**Outstanding item, not addressed by this regeneration**: production's `build_offensive_team_strength()` (used by `run_team_aggregation()` to build the canonical `team_strength_2025.csv`) still hardcodes QB=SOS-adjusted, even though every rebuilt approach here confirms baseline is now the better choice. Flagged, not changed - a deliberate scope boundary (this regeneration only touched Task 3/4/Parallel A/Parallel B/Improvement 1, not the production formula itself).

## Production Update: Baseline QB
- [x] Fixed `build_offensive_team_strength()` (production's canonical formula, used by `run_team_aggregation()`) - QB switched from SOS-adjusted to baseline `predicted_epa_per_play`, RB unchanged (still OL-adjusted). Precise 3-line edit, not a file-wide string replace (which would have corrupted 4 unrelated matches: 3 explanatory docstrings from the earlier regeneration subtasks, plus WR's intentionally-still-SOS-adjusted column in `build_full_roster_aggregation()` - confirmed by grep before editing).
- Regenerated team_strength_2025.csv, win_projections_2025.csv, win_projections_vs_actual_2025.csv.
- RESULTS: offensive_strength corr +0.306->+0.327, net_strength +0.283->+0.289 (recovers most of the SOS-bug-fix dip, consistent with the pattern already seen in Task 3/Parallel B). Win backtest correlation ~unchanged (0.221->0.216, noise-level - win-level accuracy is fairly insensitive to this one input, same finding as the original SOS bug fix task).
- Production (+0.327) is now close to but still below the Phase 3 Rebuild Regeneration's winning approach (Task 3/Parallel B's availability-blended formula, +0.454) - production still doesn't include availability/backup blending, just single-starter values. That gap was already known and flagged, not something this task addressed.

## Phase 1: Setup & Data Pipeline
- [x] Task 1.1: Project Scaffolding & Git Setup
- [x] Task 1.2: Data Collection - Play-by-Play & Player Stats
- [x] Task 1.3: Data Cleaning & Standardization
- [x] Task 1.4: Vegas Lines & Game Results Historical Data
- [x] Task 1.5: Feature Engineering - Age Curves & League Baselines

## Phase 2: Tier 1 Position Models
- [x] Task 2.1: QB Model
- [x] Task 2.2: WR Model
- [x] Task 2.3: RB Model
- [x] Task 2.4: EDGE Model

## Phase 2.5: Tier 2 Defensive Position Models (added - needed for full Defensive Strength in Phase 3)
- [x] Task 2.5: DL Model (sacks)
- [x] Task 2.6: LB Model (combined tackles)
- [x] Task 2.7: CB Model (combined tackles)
- [x] Task 2.8: S Model (combined tackles)

## Phase 2.X-Team: WAR Framework (hybrid scope - real PBP attribution only, no invented decomposition weights)
- [x] Task 1: Pass-Rush WAR (EDGE/DL) from real PBP sack/QB-hit attribution
- [x] Task 2: Team Defense EPA Prediction model
- [x] Task 3: Retrain EDGE/DL on WAR + generate projections

## Phase 2.X-Defense: Leverage EPA Attribution (Task 2's efficiency-metric proxies evaluated and not built - see Task 1 report)
- [x] Task 1: Leverage EPA Attribution (all defensive positions, real on-field rosters via defense_players)

## Phase 2 Refinement: Offensive EPA + Defensive Blending
- [x] Task 1: Retrain QB/WR/RB on EPA Per Play (not raw yards)
- [x] Task 2: Blend CB/S/LB (tackle efficiency + leverage WAR), ratio chosen per position via 2024 holdout search
- [x] Task 3: DL model clarified - keep sacks-based (R2=0.399), WAR-based (R2=0.283) discarded (EDGE keeps WAR - opposite call, tuned per position not copied)
- [x] Task 4.1: OL Quality metrics (sack rate allowed + rush EPA generated, real PBP - not PFF/PFR "Adjusted Line Yards", which isn't actually free PFR data)
- [x] Task 4.2: OL adjustment applied - empirically near-zero for QB/WR, real modest improvement for RB (data disagreed with the spec's assumed QB-most-dependent ordering)
- [x] Task 5.1: SOS adjustment (trained pass/rush defense-split models + empirical weights) - real improvement for QB, no help for WR, actively hurts for RB (don't use for RB)
- [x] Task 5.2: Availability factor (real games-played history, not hardcoded injury rates) - added as informational column for Phase 3's volume aggregation, NOT multiplied into the rate metrics; validated real 2025 accuracy strongly beats the implicit "assume full season" baseline every prior task made
- [x] Task 5.3: CB/S pass-rush synergy tested via residual regression + spurious-correlation control - CB shows no real signal, S shows a real (non-confounded) but too-weak-to-generalize signal that hurts 2024 holdout accuracy; neither adjustment recommended for use
- [x] Task 5.4.1: Coach quality (head coach, not fabricated OC/DC data) via real offense+defense EPA models - good face validity (Andy Reid/Bruce Arians top, Matt Patricia/Joe Judge bottom) but FAILS split-half reliability check (corr ~0, even negative for defense) - not a persistent signal, flagged before any forward projection

## TE Model - Phase 2 Gap Fill
- [x] TE added to OffenseEpaModel (min_opportunities=30, use_qb_context=True) - real 2025 corr=+0.311, R2=0.072, beats WR's own real-2025 result (+0.146/0.021); confirms the gap Diagnostic 1 found is now filled

## Phase 3 Rebuild
- [x] Task 1: Threaded TE through OL/SOS/availability/synergy adjustments (reused existing leak-free pipelines, not the spec's bespoke same-data-fit-and-test sketch). Real 2025 results: OL adjustment MAE 0.1959->0.1940 (modest help, like RB), SOS MAE ->0.1944 (help, like QB - TE faces pass defense), availability beats "assume full season" baseline (0.179 vs 0.227, like every position), synergy (TE's own team pass-rush WAR) MAE ->0.1888/R2 0.070->0.119 (real, out-of-sample help - stronger than CB/S's own synergy result, though the causal story is weaker than CB/S's coverage-time argument, so treat with some caution despite passing the check). Fixed an availability-adjustment idempotency bug (validate_availability ran before stale columns were dropped, causing a merge-suffix KeyError on rerun) surfaced while re-running the pipeline; also reconciled ol_quality.py's TE min_opportunities (was 20, now matches the finalized 30) and fixed compute_real_epa_per_play's TE branch (was falling into RB's rush+receiving logic instead of WR-style receiving-only).

- [x] Task 2: Position-specific SOS - verified already fully satisfied by Task 5.1/Task 1's existing POSITION_SOS_METRIC design (QB/WR/TE->pass_epa_allowed, RB->rush_epa_allowed, leak-free, validated real 2025); no new code needed, spec's proposed rebuild would have reintroduced a circular fit-on-2025 bug
- [x] Task 3: Backup depth scenarios (QB/RB only - the 2 positions production offensive_strength actually uses; WR/TE skipped since Diagnostic 2 already found adding WR hurts and hasn't been re-tested). Fixed the spec's iloc[0]/iloc[1] "starter/backup" bug (projection files aren't sorted starter-first) by reusing team_aggregation.py's own already-established "starter = highest projected value" convention; backup fallback uses a real league-average backup-tier value (QB -0.030, RB -0.062, empirically derived) instead of the spec's asserted constants. VALIDATION FINDING: availability_adjusted scenario (blends starter/backup by real availability_factor) beats production's full-season-assumption on real 2025 outcomes: corr +0.464 vs +0.341 - a genuine, out-of-sample improvement worth considering for production, not yet applied there (this task only builds the scenario files, doesn't change team_strength_2025.csv).

- [x] Task 4: Scientific offensive weight optimization - fixed the spec's inverted fit direction (it fit on 2025 alone, n=32, then "backtested" on history; flipped to fit pooled across 278 historical team-seasons 2016-2024 using each position's already-trained model applied at every historical ref_season, real 2025 held fully out as the decisive check, matching every other weight-fit in this project). WINNER by real 2025 holdout: full QB+RB+WR+TE regression, corr=+0.368 (beats QB-alone's +0.351 and production's +0.341). This reverses Diagnostic 2's "adding WR hurts" finding - that was true for the naive fixed-ratio blends tested there, not for properly OLS-optimized continuous weights; TE also contributes for the first time now that it has a model. Still short of Task 3's availability_adjusted result (+0.464) - the two ideas (optimized weights + availability blending) haven't been combined yet, flagged as a future option, not built.

- [x] Task 5: Validate against Vegas - fixed the spec's implied-win-probability formula (it treated a per-game point total (`total_line`) as if it were a season win total - unrelated markets, and the dataset has no season win-total line at all). Rebuilt from real per-game moneylines (0 missing, 272 REG games), devigged and summed per team for a genuine Vegas-implied season win total. Results: model vs. Vegas agreement corr=+0.519; vs. REAL 2025 outcomes, Vegas corr=+0.798/MAE=1.78 clearly beats our model's corr=+0.224/MAE=2.89 (expected - Vegas prices in-season info a preseason-only model can't see). Useful new context: Vegas itself is compressed vs. real variance too (std 2.13 vs real 3.46, ~1.6x) but nowhere near as compressed as our model (std 1.03, ~3.4x, confirming Task 4.1's finding) - so some compression is inherent to any preseason projection, but roughly half of our model's gap looks fixable.

## Phase 3 Final Improvements
- [x] Improvement Task 1: Combined Task 3's availability blending + Task 4's optimized weights. Fixed 2 spec column-name bugs (optimized_offensive_weights.csv is qb_epa/rb_epa/wr_epa/te_epa/intercept, not qb/rb/wr/te; team_strength_2025.csv is defensive_strength_allowed, not defensive_strength) and reused build_depth_scenarios() instead of re-deriving the blend inline. RESULT: corr=+0.444 - BETWEEN Task 3 (+0.464) and Task 4 (+0.368) alone, i.e. combining does NOT beat Task 3 alone on correlation (spec's own "~+0.50+" expectation not met), though it does post the best MAE yet (0.0645 vs. Task 3's 0.0672). Best single approach for correlation remains Task 3's availability_adjusted scenario on its own.

- [x] Parallel Task A: Full-roster depth aggregation (corrected data sources: season_team_from_weekly() crosswalk since player_season_stats.csv has no team column; targets not receiving_targets; avg_snap_pct for RB workload since no snaps column exists; fixed a real spec bug where the RB loop's break made "top-3 snap-share weighting" dead code, silently degrading RB to the same starter+backup blend as Improvement Task 1). RESULT: corr=+0.376 - below both Task 3 (+0.464) and Improvement 1 (+0.444), though above Task 4 alone (+0.368). Genuinely useful finding: compression ratio improved to 2.39x (vs. production's ~3.4x, Vegas's ~1.6x) - full-roster depth doesn't improve rank accuracy but does meaningfully widen the predicted spread, directly addressing the compression problem Task 4.1/Vegas-validation flagged, even without an accuracy win.

- [x] Parallel Task B: Availability + simple formula (fixed 3 spec bugs: depth_scenario CSVs carry the value directly as availability_adjusted, not predicted_epa_per_play_sos/ol_adjusted; team_play_mix_2024.csv doesn't exist as a file, compute_team_play_mix() called directly; used phase3_diagnostic.compute_real_2025_team_epa() for both real offense+defense EPA). RESULT: corr=+0.464 - an EXACT match to Task 3's own availability_adjusted scenario, because this is mathematically the same computation (same QB/RB availability blend, same play-mix formula) - not new information, but a clean confirmation. FINAL ANSWER to "does simplicity beat complexity": across every variant tried in Phase 3 Rebuild + Final Improvements + this parallel exploration (production +0.341, optimized weights +0.368, full-roster depth +0.376, combined +0.444, simple-with-availability +0.464), Task 3's original simple QB+RB play-mix formula with availability blending remains the single best-performing approach on real 2025 outcomes. Every attempt to add complexity (multi-position weights, full-roster depth, combining both) came in below it.

## Phase 3: Aggregation & Win Models
- [x] Task 3.1: Team Strength Aggregation (2025 target, leak-free from 2024 data; validated corr +0.34 offense / +0.33 defense vs real 2025 outcomes)
- [ ] Task 3.2: Win Prediction Models
- [ ] Task 3.3: Vegas Comparison & Edge Detection

## Phase 4: Vegas Backtesting & Validation
- [x] Task 4.1: EPA->win_pct conversion empirically fit (R2=0.747 on real 2015-2024 data) and applied to 2025 team strength - flagged: Phase 3's projected epa_diff is ~3.2x more compressed (std 0.043) than real realized team-season epa_diff (std 0.137), so projected win spread (3.8 wins) is narrower than a typical realized season (~10-13 wins) - carried into Task 4.2's calibration check
- [x] Task 4.2: Backtest vs real 2025 results - compression prediction CONFIRMED (3.36x actual vs 3.2x predicted), but overall fit weak (corr=0.224, R2=0.050, MAE=2.89 wins) - root cause is Phase 3's team strength (corr~0.33 with real team EPA) compounding through an otherwise-excellent win conversion (R2=0.747 standalone), not a Task 4 problem. Biggest misses (NE/JAX way underprojected, ARI/NYJ/LV/TEN way overprojected) look like idiosyncratic surprise seasons, not a directional bias. Phase 3's aggregation is the priority for improvement, not Task 4's conversion model.

## Phase 3 Diagnostic: Tracing Signal Loss (triggered by Phase 4's weak 0.224 win correlation)
- [x] Diagnostic 1: Individual player accuracy - EDGE (corr+0.60)/LB (+0.56) far stronger than any offense position; QB best offense at +0.41; RB/WR weak (<0.18); TE has NO model at all (confirmed gap, not an oversight). Key lead: defense's strong individual signal isn't reaching team_strength_2025's defensive_strength_allowed (which only hit +0.33), since that column comes from the team-level model, not bottom-up EDGE/LB aggregation.
- [x] Diagnostic 2: Aggregation confirmed as the bottleneck (net_strength corr 0.303 vs real, compounds to ~0.262 through the already-strong win conversion, close to Phase 4's observed 0.224). SURPRISE: bottom-up defensive composite from EDGE/LB (strong individually) is WORSE at team level (corr -0.06) than the current top-down team_defense_epa model (+0.33) - individual accuracy did not translate to team-level aggregation. CB team avg blended_score even NEGATIVELY correlated (-0.28), likely the "shutdown corners get low volume" inversion already flagged in Task 2. Offense: QB alone beats QB+RB blend (+0.361 vs +0.341); adding WR in any tested form hurts (-0.10).

## Phase 5: Automation & Infrastructure
- [ ] Weekly automated pipeline

## Phase 6: Dashboard & Polish
- [ ] Streamlit dashboard
